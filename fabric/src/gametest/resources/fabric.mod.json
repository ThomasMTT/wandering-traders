{
  "schemaVersion": 1,
  "id": "test",
  "version": "${version}",
  "name": "${displayname}",
  "icon": "${logo}",
  "environment": "*",
  "entrypoints": {
    "fabric-gametest": [
      "${tests}"
    ]
  },
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": "~${minecraft_version}",
    "java": ">=${java_version}",
    "fabric-api": "*"
  }
}